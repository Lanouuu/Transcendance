import { board, player, player2, ball } from "./script.js";

    // fastify.post("/state", async (request, reply) => {
    //     reply.send({board: board, player: player, player2: player2, ball: ball});
    // });